import "./globals.css";

export const metadata = {
  title: "Submit Your EOD (End of Day Report)",
  description: "Submit Your EOD (End of Day Report)",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={``}
      >
        {children}
      </body>
    </html>
  );
}
